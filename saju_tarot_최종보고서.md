# 청단사주타로 — 작업 보고서

**작성일** 2026-09-04 · **PRD 버전** v2.25 · **저장소** https://github.com/SeGwonKIM/Saju_Tarot

이름·생년월일·출생시각을 입력하면 **사주팔자와 타로를 결합해** 사주 풀이 · 이번 달 흐름 세 줄 ·
주제별 조언을 한 장으로 만들어 주는 웹서비스.

> **이 문서의 원칙** — 계획이 아니라 **실제로 한 일**을 적는다.
> 각 작업은 **상황 → 실행 → 확인 → 결과** 순으로 기록하고, 확인 방법을 함께 남긴다.

**관련 문서** — 시스템 구성(비기능 요구 · 서버 연결 · 보안 · 프론트/백엔드 구동)은
[`saju_tarot_시스템구성설명서.md`](saju_tarot_시스템구성설명서.md) 참조.

---

## 목차

1. [작업 실행 내역 — flow별 상세](#1-작업-실행-내역--flow별-상세)
2. [최종 PRD (v2.25)](#2-최종-prd-v225)

---

# 1. 작업 실행 내역 — flow별 상세

2026-09-04 작업분. 각 flow 는 **상황 → 실행 → 확인 → 결과** 순으로 적는다.

## Flow 1. 클라우드 배포 설정 구축

**상황** — 내 PC 를 꺼도 살아 있는 고정 주소가 필요했다. 저장소에 배포 설정이 없었다.

**실행**

| 파일 | 역할 |
|---|---|
| `Dockerfile` | 2단계 빌드 — node 로 프론트 빌드 → python 이미지에 얹기 |
| `render.yaml` | Render Blueprint — 환경변수·헬스체크·자동배포 |
| `.dockerignore` | `.env`·`backend/data/`·`*.db` 를 이미지에서 차단 |

**작업 중 발견한 결함 2건**

1. **`openai` 패키지가 `requirements.txt` 에 없었다.** `report_service.py` 가 import 하는데
   목록에 빠져 있었다. 로컬에는 수동 설치돼 있어 문제가 안 보였지만, **깨끗한 환경에서는
   리포트 생성이 ImportError 로 죽는다.** `openai==3.7.0` 으로 고정 추가.

2. **목업이 기본 켜짐이었다.** `readings.ts` 의 `VITE_USE_MOCK !== 'false'` 라서,
   빌드할 때 명시적으로 끄지 않으면 **배포본이 가짜 리포트를 보여준다.**
   Dockerfile 빌드 단계에서 끄도록 했다.

**코드 수정** — SQLite 위치가 하드코딩돼 있어 영구 디스크를 붙일 수 없었다.
`DB_PATH` 설정을 추가해 교체 지점을 열었다.

**확인** — 실제로 이미지를 빌드하고 컨테이너로 띄워 검증했다.

| 검사 | 결과 |
|---|---|
| 이미지 빌드 | 성공, 번들 비밀값 검사 통과 |
| `/api/v1/health` | `{"status":"ok","env":"production"}` |
| 만세력 API | `1990-05-15 음력` → `1990-06-07 / 경오년 임오월 계묘일` |
| 번들에 `localhost` | **0건** — 상대 주소로 잡힘 |
| 번들에 목업 데이터 | **0건** |
| 볼륨 재시작 후 DB | 유지됨 (`readings`, `share_links` 테이블 확인) |
| 테스트 | 184 passed |

**결과** — 커밋 `eee7374`.

---

## Flow 2. 서버 기동과 인터넷 공개

**상황** — 서비스를 다른 사람에게 보여줘야 했다.

**실행 순서**

```
① 로컬 서버 기동         → localhost:8000
② 공개 모드 확인          → APP_ENV 가 local 이면 명세가 열린다
③ 운영 모드로 재기동      → /docs, /openapi.json 차단 확인
④ Cloudflare Tunnel 기동  → https://….trycloudflare.com
⑤ 밖에서 접속 확인
```

**②에서 잡은 것** — 서버가 `local` 모드로 돌고 있어 **API 명세(`/openapi.json`)가 통째로
열려 있었다.** 그대로 터널을 뚫으면 전체 엔드포인트 구조가 공개된다.
운영 모드로 다시 띄워 차단을 확인한 뒤 공개했다.

**확인** — 공개 주소에서 화면 200, health 200, 만세력 API 정상, 명세 차단.

---

## Flow 3. 결함 진단 ⑧ — 문장 생성이 조용히 빠지던 문제

**상황** — 손님 화면에 *"해석 문장이 아직 없습니다"* 만 나왔다.

**진단 과정**

```
① 서버 로그 확인
   → POST /api/v1/readings 가 201 을 반환했다. 에러가 없다.
   → 그런데 응답 시간이 33ms 다. LLM 을 부르면 수 초가 걸린다.
   → 결론: LLM 호출 자체가 일어나지 않았다.

② 호출 분기 확인 (routers/readings.py)
   → 키가 있으면 OpenAIReportGenerator, 없으면 NullReportGenerator
   → 키가 비어 있다는 뜻이다.

③ 설정 로딩 확인 (config.py)
   → env_file=".env"  ← 상대경로
   → pydantic-settings 는 이것을 프로세스의 작업 위치 기준으로 찾는다
   → 서버는 C:\AI-Agent 에서 돌고, 파일은 backend/.env 에 있다
   → 못 찾는다. 그런데 에러가 아니라 빈 값이 된다.
```

**이 결함의 성격** — 에러도 경고도 나지 않는다. 키가 없으면 **조용히 계산 결과만 내보내도록**
설계돼 있기 때문이다(PRD §11.3 의 의도된 동작). **응답 시간 33ms 가 유일한 단서였다.**

**추가 발견** — `서버_실행.ps1` 도 프로젝트 루트에서 uvicorn 을 띄우므로 **같은 문제를 겪는다.**
README 의 개발 방식(`cd backend` 후 실행)에서만 우연히 동작하고 있었다.

**조치** — `env_file` 을 절대경로로 고정했다.

```python
env_file=Path(__file__).resolve().parents[1] / ".env"
```

**확인** — 서버를 재기동하고 공개 주소로 실제 리포트를 생성했다.

```
응답 44초 (수정 전 33ms)
사주풀이 3줄 · 이번달 흐름 3줄 · 주제별 조언 2건 생성됨
첫 문장: "병화 일간이라 밝게 비추는 결이 보입니다."
```

---

## Flow 4. 보안 점검 (침투 테스트)

**상황** — 인터넷에 공개된 상태였으므로, 말이 아니라 **실제로 요청을 보내** 확인했다.

**점검 항목과 방법**

| 점검 | 방법 | 결과 |
|---|---|---|
| 비밀 파일 노출 | `/.env`, `/data/field_key.txt`, `/data/readings.db` 등 7개 요청 | 전부 SPA 반환 — **내용 노출 없음** |
| 경로 탈출 | `../`, URL 인코딩, `....//`, `assets/../../` 4가지 | 전부 차단 |
| 관리자 창구 | 토큰 없이 / 틀린 토큰 / 올바른 토큰 3가지 | 앞 둘 404, 마지막만 200 |
| 명세 노출 | `/docs`, `/openapi.json` | SPA 반환 (차단) |
| 공유 링크 개인정보 | 실제 공유 링크 생성 후 응답 검사 | 이름·생년월일시 **둘 다 없음** |
| 보안 헤더 | 응답 헤더 확인 | CSP·HSTS·X-Frame-Options·nosniff 정상 |

**상태 코드만 보면 오판한다** — `/.env` 요청이 **200** 을 반환했다. 그러나 내용을 열어 보니
SPA 의 `index.html` 이었다. 상태 코드가 아니라 **본문을 확인해야** 판정할 수 있다.

---

## Flow 5. 결함 진단 ⑨ — 레이트리밋 우회

**상황** — Flow 4 에서 `rate_limit.py` 를 읽다가 IP 판정 방식이 걸렸다.

**문제**

```python
forwarded = request.headers.get("x-forwarded-for")
if forwarded:
    return forwarded.split(",")[0].strip()      # ← 맨 앞
```

`X-Forwarded-For` 의 **맨 앞자리는 요청자가 직접 채운다.** 앞단 프록시는 진짜 주소를
**뒤에** 덧붙일 뿐이다. 따라서 헤더만 갈아끼우면 매 요청이 다른 IP 로 집계된다.

**실증** — LLM 비용을 쓰지 않도록 **본문을 일부러 틀리게** 보내 카운터만 건드렸다.
(레이트리밋은 미들웨어라 본문 검증보다 먼저 실행된다.)

```
같은 IP 를 주장하며 11번  → 400 ×10, 429      ← 제한은 동작한다
IP 만 바꿔서 3번          → 400 400 400       ← 그냥 통과 (우회 성공)
```

**조치** — 믿는 순서를 바꿨다.

| 순위 | 값 | 이유 |
|---|---|---|
| 1 | `CF-Connecting-IP` | Cloudflare 가 **덮어쓴다.** 요청자가 넣어도 지워진다 |
| 2 | `X-Forwarded-For` 의 **맨 뒤** | 앞단 프록시가 덧붙인 자리 |
| 3 | 소켓 주소 | 프록시가 없는 구성 |

**테스트 수정** — `test_forwarded_header_is_used` 가 **옛 취약 동작을 못박고** 있어 깨졌다.
이번 수정이 노린 지점이므로 새 동작 기준으로 고치고, **위조 시나리오와 직접 접속 경우를
테스트로 추가**했다.

**확인** — 공개 주소에서 같은 시험을 반복했다.

```
수정 전  IP 바꿔가며 12번 → 전부 통과                ✗
수정 후  IP 바꿔가며 12번 → 400 ×10, 429, 429       ✓
```

**결과** — 커밋 `2b9313d`. 전체 테스트 **186 passed**.

---

## Flow 6. 문서 정정 (PRD v2.24)

**상황** — PRD 가 계획 그대로 남아 있어 실제 코드와 갈라져 있었다.
특히 §14.6 이 **"`X-Forwarded-For` 첫 값을 쓴다"** 고 적고 있었다 —
문서가 **틀린 것을 지시하고** 있었고, 그대로 따르면 Flow 5 의 취약점이 재발한다.

**정정 내역**

| 절 | 계획 | 실제 |
|---|---|---|
| §4.4 | Supabase PostgreSQL + Auth | **SQLite, 로그인 없음** → §4.4.1 신설 |
| §4.5 | 프론트/백엔드/DB 3분할 | **단일 Docker 이미지** |
| §4.7 | Claude API | **OpenAI `gpt-5.5`** |
| §12.3 | JWT 인증·인가 | 구현/미구현 구분해 표기 |
| §12.17 | 주 1회 백업 | **한 벌도 없음** 명시 |
| §13 | 리포트 8초 | **44초 실측** 기록 |
| §14.1·14.2 | Cloudflare Pages + Render | 단일 이미지 + 실제 환경변수 목록 |
| §14.6 | XFF 첫 값 | **CF-Connecting-IP → XFF 맨 뒤 → 소켓** |

**지우지 않고 옮긴 것** — Supabase·RLS·JWT 결정은 삭제하지 않고
**§4.4.2 「원래 계획 (클라우드 전환 시 복원)」** 으로 옮겼다. 나중에 로그인을 붙이거나
클라우드로 갈 때 판단 근거가 다시 필요하다.

**결과** — 커밋 `200ed9f`. 1,239줄 → 1,333줄.

---

## Flow 7. 백업 구축 (PRD v2.25)

**상황** — PRD §12.17 이 요구했으나 **백업이 한 벌도 없었다.** 손님의 이름·생년월일이
SQLite 파일 하나에만 있었고, 그 파일을 잃으면 복구 수단이 없었다.

**설계 판단 4가지**

1. **파일 복사가 아니라 SQLite 백업 API 를 쓴다.** 돌아가는 DB 를 그냥 복사하면
   쓰기 중간 상태가 찍혀 **깨진 사본**이 나온다. 서버를 끄지 않아도 된다.

2. **"떴다"가 아니라 "열린다"를 확인한다.** 뜨자마자 `integrity_check` 로 열어 보고
   건수를 센다. `--검증` 으로 나중에 다시 확인할 수 있다.
   *(픽사·GitLab 둘 다 "백업이 있다고 믿었지만 고장 나 있었다.")*

3. **복원은 덮어쓰지 않는다.** 새 폴더로 꺼내고 옮기는 것은 사람이 한다.
   복원이 데이터를 날리는 사고가 되면 안 된다.

4. **필드 암호화 키를 함께 넣는다.** `FIELD_ENCRYPTION_KEY` 가 환경변수에 없어
   `backend/data/field_key.txt` 가 자동 생성돼 있었고, **DB 와 같은 폴더**에 있다.
   폴더째 잃으면 키와 데이터를 한꺼번에 잃어 **백업이 있어도 복호화하지 못한다.**
   대신 암호가 단일 비밀이 되므로 비밀번호 관리자에 두도록 명시했다.

**암호화 방식** — scrypt(암호) → **AES-256-GCM**. 보관 28일(4주) 후 자동 삭제.

**확인** — 실제 데이터로 전 과정을 돌렸다.

```
✓ 백업   리포트 28건 · 공유링크 2건 · 13KB   (원본 143KB → 압축+암호화)
✓ 검증   열림, 건수 일치, 필드 키 포함
✓ 복원   28건 · 2건 — 건수 일치
✗ 틀린 암호 → 거부 (스택 트레이스가 아니라 안내 문구로)
```

**결과** — 커밋 `66fd7c1`. E: 드라이브(C: 와 물리적으로 다른 디스크)에 사본 배치 완료.

**남은 것 (사람이 해야 하는 부분)**
- **off-site 한 벌** — 외장하드·클라우드로. 안 하면 3-2-1 의 "1"이 빈다
- **분기 1회 `--검증`** — 열어본 적 없는 백업은 백업이 아니다

---

## 작업 요약

| Flow | 산출물 | 커밋 |
|---|---|---|
| 1 | Dockerfile · render.yaml · .dockerignore (+ 결함 2건 수정) | `eee7374` |
| 2 | 서버 기동 · 터널 공개 (명세 노출 차단) | — |
| 3 | `.env` 미로딩 수정 | `2b9313d` |
| 4 | 침투 점검 (6개 항목) | — |
| 5 | 레이트리밋 우회 수정 + 테스트 개정 | `2b9313d` |
| 6 | PRD 정정 (8개 절) | `200ed9f` |
| 7 | 백업·복원 도구 | `66fd7c1` |

**테스트** 184 → **186 passed** (위조 시나리오 2건 추가)
**차단한 취약점** 누적 **9건**

---

# 2. 최종 PRD (v2.25)

전문: [`docs/PRD.md`](https://github.com/SeGwonKIM/Saju_Tarot/blob/main/docs/PRD.md) — 1,333줄 / 20장

## 2.1 문서 구조

| 장 | 내용 |
|---|---|
| 0 | 요구사항 반영 판정 |
| 1–3 | 배경과 목표 · 대상 사용자 · 범위 |
| 4 | 기술 스택 결정 **(+ §4.4.1 실제 구현 / §4.4.2 원래 계획)** |
| 5 | 시스템 설계 |
| 6–7 | 화면·UX 명세 · 목업 데이터 전략 |
| 8 | 도메인 로직 명세 (만세력·절기) |
| 9 | 데이터 모델 |
| 10 | API 명세 (계약 문서) |
| 11 | AI 해석 레이어 |
| 12 | **보안 요구사항** (20개 항목) |
| 13 | 비기능 요구 **(+ 실측값)** |
| 14 | 배포·운영 **(단일 이미지로 개정)** |
| 15–17 | 개발 순서 · 테스트·검증 · 리스크 |
| 18–20 | 빈칸 채우기 · 변경 이력 · 참고 |

## 2.2 v2.20 이후 변경 이력

| 버전 | 내용 |
|---|---|
| **v2.20** | 내 PC 서버 운영 방식 추가 (§14.6). 단일 오리진 + Cloudflare Tunnel. 레이트리밋 구현. `/openapi.json` 노출 차단 |
| **v2.21** | 저장·공유 구현. **Supabase → SQLite.** 이름·생년월일 AES-256-GCM 암호화. **테스트가 문제 2건을 잡음** — `secure_delete` 필요, 공유 payload 의 생년월일시 제거 |
| **v2.22** | 공개 전 침투 점검 (§12.20 신설). **취약점 4건 차단** — 무인증 목록 노출, 보안 헤더 전무, 로그 원문 노출, 빌드에 localhost |
| **v2.23** | 2차 점검. **3건 추가 차단** — 무인증 삭제, 요청 크기 상한 부재, `.gitignore` 나열식 |
| **v2.24** | **문서와 구현의 어긋남 정리 + 조용히 실패하던 결함 2건.** §4.4.1 신설, 배포 방식·LLM 공급자 정정, `.env` 미로딩 수정, **레이트리밋 우회 수정** |
| **v2.25** | **백업 구현 (§12.17).** SQLite 백업 API + `integrity_check` + AES-256-GCM. 복원은 덮어쓰지 않음. 실측: 28건 백업→검증→복원 성공 |

## 2.3 문서가 실제와 어긋났던 지점 (v2.24 에서 정정)

이 프로젝트에서 얻은 교훈 중 하나다 — **문서가 오래되면 틀린 것을 지시한다.**

| 어긋남 | 위험 |
|---|---|
| §14.6 "XFF 첫 값을 쓴다" | 그대로 따르면 **레이트리밋이 무력화**된다 |
| §4.4 "Supabase + RLS" | 실제로는 SQLite — RLS 를 켰다고 착각할 수 있다 |
| §4.7 "Claude API" | 실제는 OpenAI — 키를 잘못 발급하게 된다 |
| §12.17 "주 1회 백업" | 실제로는 **한 벌도 없었다** |

## 2.4 남은 과제

| 우선순위 | 항목 |
|---|---|
| 🔴 높음 | **off-site 백업 한 벌** — 현재 사본이 모두 같은 PC 안에 있다 |
| 🔴 높음 | **OpenAI 예산 상한 설정** — 레이트리밋은 시간당 한도라 최종 방어선이 아니다 |
| 🟠 중간 | **리포트 응답 44초** — 기준 8초 초과. `max_tokens` 미설정 |
| 🟠 중간 | Render 배포 완료 (대시보드 작업 필요) |
| 🟡 낮음 | 로그인 도입 시 인증·인가 구현 (§12.3) |
| 🟡 낮음 | CI 취약점 스캔 자동화 |

---

*본 문서의 모든 수치는 2026-09-04 실측값이며, 검증 방법을 각 Flow 에 함께 기록했다.*
